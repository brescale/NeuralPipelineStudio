========================================================================================
   NEURAL PIPELINE STUDIO v2.1 - PORTABLE HOME EDITION (MULTI-GAME & MULTI-PC)
========================================================================================

This distribution is 100% self-contained, portable, and designed around a 'HOME-FIRST' workflow.
You can extract this folder anywhere: on your Desktop, on an external USB drive, or on any PC.
No installation, .NET runtime installation, or admin privileges are required.

WORKFLOW & KEY FEATURES:
----------------------------------------------------------------------------------------
1. HOME SCREEN & AUTOMATED PC GAMES SCANNER:
   - On launch, the application opens directly to the 'Home Games Hub' without being
     pre-locked to any specific title.
   - Click '🔍 Scan PC Games': the software rapidly scans all connected storage drives
     (Steam, Xbox Game Pass, Epic Games, GOG libraries) and creates interactive cards
     for every detected game.
   - Each card displays the game title, engine (Rockstar RAGE, Unreal Engine, REDengine,
     Creation Engine), graphics API (DirectX 12, Vulkan, DirectX 11), Anti-Cheat status,
     and active injection badges.

2. DEDICATED WORKBENCH PER GAME:
   - Click '🎛 Configure Game & Pipeline' on any game card:
     the app immediately opens the DEDICATED WORKBENCH for that specific title!
   - The top banner shows the active game with quick actions:
     ('🚀 Inject', '🧹 Vanilla Restore', '▶ Launch Game', '💾 Save Config')
     alongside the '⬅ Back to Home' navigation button.
   - Dedicated sidebar modules:
     * '🎛 Pipeline Matrix': Inspect, reorder, loop, and configure rendering passes.
     * '⚡ Neural & DLSS': Configure DLSS models (from 2 up to 5.0), RenoDX HDR tensor
       passes, and DLSS-NR neural denoiser parameters.
     * '📊 VRAM & Telemetry': Real-time GPU memory tracking calibrated to the 80-85% sweet spot.
     * '💾 Presets & Profiles': Load or export certified hardware profiles.
     * '📦 DLSS Models': Manage DLL models associated with the active game.

3. LANGUAGE TOGGLE (ENGLISH / ITALIAN):
   - Click the '🇬🇧 EN' or '🇮🇹 IT' button in the top-right corner of the header bar to
     instantly toggle the entire user interface and documentation.
   - Your language preference is automatically saved in 'studio_settings.json'.

4. HARDWARE TELEMETRY & AUTO-CALIBRATION FOR ANY PC:
   - The live banner at the top displays your GPU, driver version, VRAM, CPU, and RAM.
   - Click '⚡ Auto-Calibrate PC': the studio automatically calculates the optimal
     pipeline parameters (cycles, loop counts, tensor passes, DLSS scaling, and VRAM cap)
     tailored specifically to your machine:
     * Enthusiast (16-24 GB VRAM): RTX 4090 / 4080 (15x cycles, 1000% Super Resolution)
     * High-End (10-12 GB VRAM): RTX 4070 / 3080 (10x cycles, 900% Super Resolution)
     * Mainstream (8 GB VRAM): RTX 4060 / 3070 / RX 6700 (6x cycles, 600% Super Resolution)
     * Entry (4-6 GB VRAM): RTX 3050 / GTX 1660 (3x cycles, 400% Super Resolution)
     * Non-RTX: AMD Radeon & Intel Arc (OptiScaler FSR 3.1 / XeSS 1.3 Universal bridge)

5. BUILT-IN TECHNICAL WIKI:
   - Click '📚 Studio Wiki & Docs' in the sidebar to access all 8 in-depth chapters
     explaining neural tensor rendering, ping-pong math, VRAM allocation, and troubleshooting.

FOLDER STRUCTURE:
----------------------------------------------------------------------------------------
- NeuralPipelineStudio.exe : Standalone executable (built-in .NET 8 LTS runtime)
- presets/                 : Certified hardware-tier JSON presets
- payload/                 : Complete injection suite (DLL proxies, ReShade shaders, models)
- LEGGIMI_GUIDA_RAPIDA.txt : Italian quickstart guide
- QUICKSTART_EN.txt        : English quickstart guide

Enjoy the ultimate AAA neural rendering experience!
========================================================================================
